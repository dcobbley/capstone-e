/**
 * @access public
 * @description Restore every data type set as true in settings
 * @param {callback} oncomplete
 */
var restore = function(oncomplete) {

};

// Defines Ffosbr restore
module.exports = restore;
